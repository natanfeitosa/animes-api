const { all_animes } = require('./all_animes')
const { anime } = require('./anime')
const { category } = require('./category')
const { episode } = require('./episode')
const { home } = require('./home')
const { search } = require('./search')

module.exports = {
  all_animes,
  anime,
  category,
  episode,
  home,
  search
}
